function plus(n1, n2) {
  return n1 + n2;
}

function f3(valueA, valueB) {
  if (valueA === "test" && valueB !== "temp") {
    return valueB;
  } else {
    return " " + valueB + "random text";
  }
}

let totalCat = 0;
